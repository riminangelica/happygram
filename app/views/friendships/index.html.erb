<div class="page-header">
  <h1>My Friends</h1>
</div>

<ul class="nav nav-pills" id="friend-list-filter">
  <li class="<%= 'active' if params[:list] == '' || params[:list].blank? %>"><%= link_to 'All', friendships_path %></li>  
  <li class="<%= 'active' if params[:list] == 'blocked' %>"><%= link_to 'Blocked', friendships_path(list: 'blocked') %></li>  
  <li class="<%= 'active' if params[:list] == 'requested' %>"><%= link_to 'Requested', friendships_path(list: 'requested') %></li>
  <li class="<%= 'active' if params[:list] == 'accepted' %>"><%= link_to 'Accepted', friendships_path(list: 'accepted') %></li>
  <li class="<%= 'active' if params[:list] == 'pending' %>"><%= link_to 'Pending', friendships_path(list: 'pending') %></li>
</ul>

<hr />

<div id="friend-list">
<% @friendships.each do |friendship| %>
  <% friend = friendship.friend %>
  <div id="<%= dom_id(friendship) %>" class="media friend">
    <%= link_to image_tag(friend.gravatar_url), profile_path(friend), class: 'pull-left' %>
    <div class="media-body">
      <h4 class="media-heading"><%= friend.full_name %></h4>
      <em>Friendship is <%= friendship.state %></em>. <br />
      <%= link_to "#{friendship.update_action_verbiage}", edit_friendship_path(friendship.friend.handle), class: 'btn btn-small' %>
    </div>
  </div>
<% end %>
</div>