
<% simple_form_for([@entry.comments.build, @comments]) do |f| %>
  <%= f.error_notification %>
  <div class="form-inputs">
    <%= f.input :content %>
    <%= f.input :entry_id %>
    <%= f.input :user_id %>
  </div>
  <div class="form-actions">
  <%= f.submit "Submit" %>
	</div>
<% end %>

